interface:
  display_name: "Identity-Safe Photo Editing"
  short_description: "Compose safe retouch, upscale, and export stages"
  default_prompt: "Use $identity-safe-photo-editing to route and compose this multi-stage photo workflow with explicit asset lineage."
